section .data
    str1 db "Apple", 0
    str2 db "Banana", 0

section .text
global _start

; TODO: Implement strcmp function
; Return -1 if str1 < str2, 0 if equal, 1 if str1 > str2

_start:
    ; TODO: Compare str1 and str2
    ; Exit with result + 1 (to make it positive)
