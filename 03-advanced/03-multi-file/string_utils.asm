section .text

; TODO: Export functions using 'global'

; TODO: Implement my_strlen
; Arguments: rdi = pointer to string
; Returns: rax = length

; TODO: Implement my_strcmp
; Arguments: rdi = string1, rsi = string2
; Returns: rax = 0 (equal), -1 (s1<s2), 1 (s1>s2)
